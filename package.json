{
  "name": "replace-path",
  "version": "0.1.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npm run lint && tap *.spec.js --100",
    "coverage": "tap *.spec.js --cov --coverage-report=html",
    "lint": "semistandard"
  },
  "author": "Simone Lusenti <simone@plasticpanda.com>",
  "license": "MIT",
  "devDependencies": {
    "deep-freeze": "0.0.1",
    "pre-commit": "^1.1.3",
    "semistandard": "^9.0.0",
    "tap": "^11.1.1"
  },
  "dependencies": {
    "clone": "^2.1.0"
  }
}
